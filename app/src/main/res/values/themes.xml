<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WidgetStudy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WidgetStudy" parent="Base.Theme.WidgetStudy" />
    <style name="btn" >
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/pink</item>
        <item name="cornerRadius">10dp</item>
        <item name="marginHorizontal">5dp</item>

    </style>
    <style name="theme" parent="Theme.WidgetStudy">
        <item name="android:textSize">24dp</item>
        <item name="android:textColor">#3F51B5</item>
        <item name="materialButtonStyle">@style/btn</item>
    </style>
</resources>